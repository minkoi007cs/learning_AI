# Dependencies
node_modules/

# Build outputs
dist/
.next/
build/
*.tsbuildinfo

# Env files (never commit secrets)
.env
.env.local
.env*.local
.env.test
.env.*.test

# OS / editor
.DS_Store

# Vercel
.vercel

# Agent / local orchestration state (not for deploy)
.agentui/
state/

# Log do scripts/start.sh sinh ra
.logs/

# File tam khi keo bien tu Vercel (scripts/pull-env-vercel.sh)
.vercel-pull.env

# Bản sao lưu app cũ để đối chiếu — chỉ nằm trên máy, không đẩy lên repo
_ban-cu-*/

.turbo

