# Base URL of the deployed backend API — MUST include the /v1 prefix.
# Local dev default (used when this var is unset): http://localhost:3001/v1
# On Vercel, set this in Project Settings → Environment Variables.
NEXT_PUBLIC_API_URL=https://your-backend-domain.com/v1
